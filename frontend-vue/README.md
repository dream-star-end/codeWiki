# Vue Frontend (DeepWiki Style)

## Run

```bash
cd frontend-vue
npm install
npm run dev
```

Set API Base in the UI to your backend (default `http://localhost:8000`).

## Notes

- Uses Naive UI, Mermaid, and marked.
- Requires Node 20.19+ for Vite 7 (current project may warn on 20.11).
